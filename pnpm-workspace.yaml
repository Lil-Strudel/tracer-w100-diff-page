packages:
  - apps/*

allowBuilds:
  esbuild: set this to true or false

catalog:
  typescript: ^5.9.2

# vite/vitest need esbuild's native binary, so its install script must run.
onlyBuiltDependencies:
  - esbuild
